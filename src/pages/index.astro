---
import Layout from '../layouts/Layout.astro';
---

<Layout title="The Repair Scanner – Schäden erkennen. Dringlichkeit verstehen. Sofort richtig handeln.">
  <main class="page">
   <!-- TOP NAV / HERO -->
<div class="nav-bg">
  <header class="nav">
    <div class="nav__left">
      <div class="logo_wrap">
        <img class="logo" src="/images/the-repair-scanner-logo-transparent-2x.png" alt="The Repair Scanner – powered by CURA" />
      </div>
    </div>

    <nav class="nav__center">
      <a href="#produkt">Produkt</a>
      <a href="#vorteile">Vorteile</a>
      <a href="#pilot">Pilot</a>
      <a href="#faq">FAQ</a>
    </nav>

    <div class="nav__right">
      <a class="btn btn--ghost" href="#pilot">Pilotprojekt anfragen</a>
      <a class="btn btn--primary" href="#pilot">Demo vormerken</a>
    </div>
  </header>
</div>

    <section class="hero" id="produkt">
      <div class="hero__grid">
        <div class="hero__text">
          <div class="pill">KI-Schadensanalyse • Ampel-Priorisierung • Materialliste • Online-Beratung</div>

          <h1>
            KI-gestüzte Schadensanalyse für Gebäude<br />
                    Schäden erkennen, 
            priorisieren und fundiert entscheiden.
          </h1>

          <h2>
            Modulare Plattform für Hausverwaltungen, 
            Handwerksbetriebe und Organisationen - 
            startend mit Pilot-Modulen 2026
          </h2>

          <p class="lead">
            The Repair Scanner ist eine KI-gestütze, modulare Plattform zur Analyse und Bewertung von Gebäudeschäden. Die Startmodule (Pilotbetrieb ab 2026) unterstützen 
            die strukturierte Schadenserfassung, eine Ampel-Priorisierung und die Abteilung konkreter Maßnahmen - als Basis für weitere Module und Workflows.
          </p>

          <p class="services">
            Ergebnisorientierte Auswertung mit klaren
          nächsten Schritten:
            <strong>Materiallisten</strong>,
            <strong>fachliche Beratung</strong> oder
            <strong>strukturierte Übergabe an einen passenden Fachbetrieb</strong>.
          </p>

          <div class="hero__cta">
            <a class="btn btn--primary" href="#pilot">Demo kostenlos vormerken</a>
            <a class="btn btn--ghost" href="#so-funktionierts">So funktioniert’s</a>
          </div>

          <ul class="checks">
            <li>Für Pilotprojekte optimiert</li>
            <li>Datenschutz & klare Rollen</li>
            <li>Klare nächste Schritte statt offener Schadensbewertung</li>
          </ul>

          <p class="note">
            Hinweis: Der Repair Scanner hilft dabei, Schäden an Gebäuden frühzeitig zu erkennen und klare Handlungsempfehlungen zu erhalten.
          </p>
        </div>

        <div class="hero__visual">
          <!-- Window-Mockup: NUR Handybild, kein Text rechts (steht im Bild) -->
          <div class="window">
            <div class="window__top">
              <span class="dot"></span><span class="dot"></span><span class="dot"></span>
              <span class="window__title">Repair Scanner Schadensanalyse </span>
              <span class="window__chip">Foto-Upload</span>
            </div>

            <div class="window__body window__body--single">
              <div class="window__left window__left--single">
                <div class="window__brand">The Repair Scanner</div>
                <div class="window__placeholder window__placeholder--img">
                  <!-- NEUES BILD -->
                  <img class="window__img" src="/images/1000145878.png" alt="App-Vorschau (Beispiel)" />
                </div>
              </div>
            </div>
          </div>

          <div class="mini-metrics">
            <div class="metric"><b>1</b><span>Foto/Upload</span></div>
            <div class="metric"><b>Ampel</b><span>klare Priorität</span></div>
            <div class="metric"><b>3</b><span>Handlungswege</span></div>
            <div class="metric"><b>Dashboard</b><span>für Teams & Organisationen</span></div>
          </div>
        </div>
      </div>
    </section>

    <!-- PROBLEM + ZAHLEN -->
    <section class="section section--tight" id="vorteile">
      <div class="container">
        <h2>Das Problem</h2>
        <p class="muted">
          Schäden werden zu spät erkannt, falsch priorisiert. Der Repair Scanner unterstützt bei der Bewertung von Schäden an Holzkonstruktionen, Mauerwerk und Bausubstanz - visuell, strukturiert und nachvollziehbar.
        </p>

        <div class="impact">
          <h3>Zu späte oder ungelöste Schäden</h3>
          <div class="impact__number">
            <span class="n">Hunderte bis Tausende €</span><span class="s">pro Schadenfall</span>
          </div>
          <p class="muted">Typische Zusatzkosten durch verzögerte Reparaturen, falsche Einschätzung oder unnötige Einsätze.</p>
        </div>

        <!-- 4 Zielgruppen-Karten mit BILD OHNE TEXT + HTML-Overlay -->
        <div class="cards">
          <!-- Eigentümer -->
          <article class="imgcard" style="--bg:url('/images/trs_eigentuemer.png')">
            <div class="imgcard__overlay">
              <h4>Eigentümer</h4>
              <ul>
                <li>Schnelle Klarheit statt endloser Recherche</li>
                <li>Materialliste oder Empfehlung für einen Fachbetrieb</li>
              </ul>
              <a class="btn btn--small btn--ghost" href="#pilot">Jetzt testen</a>
            </div>
          </article>

          <!-- Hausverwaltungen -->
          <article class="imgcard" style="--bg:url('/images/trs_hausverwaltung.png')">
            <div class="imgcard__overlay">
              <h4>Hausverwaltungen</h4>
              <ul>
                <li>Strukturierte Meldungen statt Chaos im Postfach</li>
                <li>Standardisierte Übergabe an Dienstleister</li>
              </ul>
              <a class="btn btn--small btn--ghost" href="#pilot">Jetzt testen</a>
            </div>
          </article>

          <!-- Handwerk -->
          <article class="imgcard" style="--bg:url('/images/trs_handwerk.png')">
            <div class="imgcard__overlay">
              <h4>Handwerk</h4>
              <ul>
                <li>Vorqualifizierte Fälle statt Blindfahrten</li>
                <li>Bessere Infos vor dem Termin</li>
              </ul>
              <a class="btn btn--small btn--ghost" href="#pilot">Pilot anfragen</a>
            </div>
          </article>

          <!-- Bevölkerungsschutz -->
          <article class="imgcard" style="--bg:url('/images/trs_bevoelkerungsschutz.png')">
            <div class="imgcard__overlay">
              <h4>Bevölkerungsschutz</h4>
              <ul>
                <li>Priorisierung sicherheitskritischer Lagen</li>
                <li>Dokumentation & Übergabe</li>
              </ul>
              <a class="btn btn--small btn--ghost" href="#pilot">Demo buchen</a>
            </div>
          </article>
        </div>
      </div>
    </section>

    <!-- DIE LÖSUNG -->
    <section class="section" id="so-funktionierts">
      <div class="container">
        <h2>Die Lösung</h2>
        <p class="muted">
          The Repair Scanner verbindet KI-Analyse mit echter Handwerkserfahrung.
          Das Ergebnis ist ein schneller, nachvollziehbarer Entscheidungsprozess – für Privatpersonen wie auch für professionelle Betreiber.
        </p>

        <div class="grid3">
          <div class="panel">
            <h3>KI-Analyse (Phase 1)</h3>
            <p>Upload von Schadensbildern und Ableitung einer Risiko-Ampel inkl. „Sicherheit der Einschätzung“. Pilot-tauglich und erweiterbar.</p>
          </div>
          <div class="panel">
            <h3>Handlungsempfehlung</h3>
            <p>Materialliste, Online-Beratung oder direkte Übergabe an Fachbetrieb/Versicherung – je nach Risiko.</p>
          </div>
          <div class="panel">
            <h3>Dashboard & Workflow</h3>
            <p>Schadensmeldungen sammeln, doppelte Meldungen reduzieren, Aufgaben zuweisen und erledigte Fälle dokumentieren.</p>
          </div>
        </div>

        <h2 class="mt">So funktioniert es im Pilotbetrieb</h2>
        <p class="muted">
          In Phase 1 starten wir bewusst schlank: Foto/Upload, KI-Analyse, Ampel-Priorisierung, nächste Schritte.
          Alles Weitere folgt in Phase 2 und 3.
        </p>

        <div class="steps">
          <div class="step">
            <div class="step__n">1</div>
            <h4>Bild erfassen</h4>
            <p>Web-MVP: Upload oder Foto. Optional mit Objektadresse/Projektzuordnung (für Verwaltungen/Organisationen).</p>
          </div>
          <div class="step">
            <div class="step__n">2</div>
            <h4>KI-Auswertung</h4>
            <p>Schadensart + Sicherheit der Einschätzung + Ampel (grün/gelb/rot). Fokus auf klare Entscheidungen statt Perfektion.</p>
          </div>
          <div class="step">
            <div class="step__n">3</div>
            <h4>Nächster Schritt</h4>
            <p>Materialliste, Online-Beratung oder Übergabe an Fachbetrieb/Versicherung. Alles wird im Dashboard dokumentiert.</p>
          </div>
        </div>
      </div>
    </section>

    <!-- MODULE + PILOT CTA -->
    <section class="section" id="pilot">
      <div class="container">
        <h2>Module (Roadmap-fähig)</h2>
        <p class="muted">
          Du kannst die Module als Erlösmodelle / Zielgruppen verstehen.
          Für Phase 1 priorisieren wir die wenigen, die Pilotumsatz & Proof liefern.
        </p>

        <div class="grid grid-cols-1 md:grid-cols-2gap-8">
          <div class="panel">
            <h3>Phase-1 Fokus (Pilot-tauglich)</h3>
            <ul class="bullets">
              <li>Modul A: Handwerksbetriebe (B2B Lizenz)</li>
              <li>Modul B: Hausverwaltungen (B2B Lizenz)</li>
              <li>Modul C: Early Pilot / Organisationen (Lizenz + Feedback)</li>
              <li>Weitere Module (Gemeinden/Katastrophenschutz, Versicherung, Online-Beratung) in Phase 2/3 skalierbar</li>
            </ul>
          </div>

          <div class="panel">
            <h3>Was kommt in Phase&nbsp;2 / 3 dazu</h3>
<ul class="future-list">
  <li>
    <strong>Live-Analyse per Kamera</strong><br />
    Schäden können direkt vor Ort erfasst und visuell markiert werden.
  </li>
  <li>
    <strong>Automatische Objekterkennung</strong><br />
    Relevante Bauteile und Schadstellen werden im Bild erkannt und hervorgehoben.
  </li>
  <li>
    <strong>Intelligente Priorisierung</strong><br />
    Mehrere Meldungen werden zusammengeführt, bewertet und doppelte Einträge vermieden.
  </li>
  <li>
    <strong>Experten- & Partnerplattform</strong><br />
    Direkte Weitergabe an Fachpersonen, Betriebe oder Organisationen.
  </li>
  <li>
    <strong>Erweiterte Norm- & Wissenslogik</strong><br />
    Unterstützung bei Einschätzung, Dokumentation und nächsten Schritten.
  </li>
</ul>
          </div>
        </div>

        <div class="cta">
          <div class="cta__text">
            <h2>Pilotpartner gesucht</h2>
            <p>
              Du bist Hausverwaltung, Organisation oder Handwerksbetrieb und möchtest 6–8 Wochen testen?
              Du bekommst eine schlanke Pilotversion, klare Meilensteine und ein sauberes Feedback-Setup.
            </p>

            <div class="cta__btns">
              <a class="btn btn--primary" href="#pilot-form">Pilotprojekt anfragen</a>
              <a class="btn btn--ghost" href="#faq">FAQ ansehen</a>
            </div>
          </div>

          <form class="form" id="pilot-form" name="pilot" method="POST" data-netlify="true">
            <input type="hidden" name="form-name" value="pilot" />
            <label>
              <span>Ihr Name</span>
              <input name="name" required />
            </label>
            <label>
              <span>E-Mail</span>
              <input name="email" type="email" required />
            </label>
            <label>
              <span>Organisation (optional)</span>
              <input name="org" />
            </label>
            <button class="btn btn--primary btn--full" type="submit">Pilotanfrage senden</button>
            <p class="tiny muted">Das Absenden speichert die Anfrage in Netlify Forms. (Kein Backend nötig)</p>
          </form>
        </div>
      </div>
    </section>

    <!-- FAQ -->
    <section class="section" id="faq">
      <div class="container">
        <h2>FAQ</h2>
        <p class="muted">Kurz und klar, wie du es später auch in Gesprächen nutzen kannst.</p>

        <details class="faq">
          <summary>Warum startet Phase 1 ohne Live-Scan?</summary>
          <p>
            Weil Foto/Upload im Web schneller pilotfähig ist und die KI-Grundlogik validiert.
            Live-Scan und Detektion folgen in Phase 2, sobald echte Pilotdaten vorliegen.
          </p>
        </details>

        <details class="faq">
          <summary>Was ist der konkrete Output der KI in Phase 1?</summary>
          <p>
            Schadensart, Ampel-Priorität (grün/gelb/rot) und die Sicherheit der Einschätzung – als stabiler Kern für spätere Material-,
            Norm- und Expertenlogik.
          </p>
        </details>

        <details class="faq">
          <summary>Wie sicher sind meine Daten?</summary>
          <p>
            Wir trennen Rollen (Privat/Team), minimieren Daten und können Speicherung sowie Retention im Pilot festlegen.
            Auf Wunsch nur temporäre Speicherung für Analyse/Feedback.
          </p>
        </details>

        <footer class="footer">
          <div class="footer__left">
            <img class="logo" src="/images/the-repair-scanner-logo-transparent-2x.png" alt="The Repair Scanner – powered by CURA" />
          </div>
          <div class="footer__right muted">© {new Date().getFullYear()} The Repair Scanner • DACH & EU</div>
        </footer>
      </div>
    </section>
  </main>

  <style>
    :root{
      --bg0:#101833;
      --bg1:#182553;
      --panel:rgba(255,255,255,.08);
      --panel2:rgba(255,255,255,.10);
      --stroke:rgba(255,255,255,.14);
      --text:rgba(255,255,255,.92);
      --muted:rgba(255,255,255,.68);
      --glow:rgba(80, 200, 255, .25);
      --primary:#4cc9ff;
    }

    .page{
      min-height:100vh;
      color:var(--text);
      background:
        radial-gradient(1100px 600px at 75% 20%, rgba(60, 220, 255, .18), transparent 60%),
        radial-gradient(900px 700px at 20% 10%, rgba(120, 255, 170, .10), transparent 55%),
        radial-gradient(800px 600px at 60% 80%, rgba(120, 120, 255, .10), transparent 55%),
        linear-gradient(180deg, var(--bg0), var(--bg1));
      padding-bottom:60px;
    }

    .container{
      width:min(1100px, calc(100% - 40px));
      margin:0 auto;
    }

/* NAV BACKGROUND STRIP */
.nav-bg{
  width: 100%;
  padding: 6px 0;

  background:
    linear-gradient(
      180deg,
      rgba(255,255,255,0.14),
      rgba(255,255,255,0.06)
    );

  backdrop-filter: blur(14px);
  -webkit-backdrop-filter: blur(14px);

  border-bottom: 1px solid rgba(255,255,255,0.12);

  box-shadow:
    0 6px 18px rgba(76,201,255,0.08);
}


    /* NAV */
    .nav{
      width:min(1100px, calc(100% - 40px));
      margin:0 auto;
      display:flex;
      flex-wrap: nowrap;
      align-items:center;
      justify-content:space-between;
      padding:18px 0;
      gap:18px;
    }
.nav__left{
  display:flex;
  align-items:center;
  flex-shrink:0;
}

.nav__center{
  display:flex;
  align-items:center;
  gap:18px;
  margin-left:18px;
  white-space:nowrap;
  flex:1;
}

.nav__right{
  display:flex;
  align-items:center;
  gap:12px;
  flex-shrink:0;
}

    .logo{ max-height:170px; width:auto; height: auto; display:block; object-fit: contain;  }

    .nav__center a{ 
      color:rgba(255,255,255,.72);
      text-decoration:none;
      font-weight:800;
      font-size:14px;
}

    /* Buttons */
    .btn{
      display:inline-flex;
      align-items:center;
      justify-content:center;
      gap:10px;
      padding:10px 14px;
      border-radius:999px;
      border:1px solid var(--stroke);
      text-decoration:none;
      color:var(--text);
      background:rgba(255,255,255,.04);
      font-weight:700;
      font-size:14px;
      box-shadow:0 0 0 0 rgba(0,0,0,0);
      transition:transform .12s ease, background .12s ease, border-color .12s ease;
      cursor:pointer;
    }
    .btn:hover{ transform:translateY(-1px); border-color:rgba(255,255,255,.18); }
    .btn--primary{
      background:linear-gradient(90deg, rgba(76,201,255,.22), rgba(120,255,170,.18));
      border-color:rgba(76,201,255,.35);
      box-shadow:0 0 24px rgba(76,201,255,.12);
    }
    .btn--ghost{ background:rgba(255,255,255,.03); }
    .btn--small{ padding:9px 12px; font-size:13px; }
    .btn--full{ width:100%; border-radius:14px; }

    .pill{
      display:inline-flex;
      padding:8px 12px;
      border-radius:999px;
      background:rgba(255,255,255,.04);
      border:1px solid rgba(255,255,255,.08);
      color:var(--muted);
      font-weight:600;
      font-size:12px;
      margin-bottom:14px;
    }

    /* HERO */
    .hero{
      width:min(1100px, calc(100% - 40px));
      margin:0 auto;
      padding:18px 0 6px;
    }

    .hero__grid{
      display:grid;
      grid-template-columns: 1.05fr .95fr;
      gap:26px;
      align-items:start;
    }

    h1{
      font-size:48px;
      line-height:1.05;
      margin:0 0 12px;
      letter-spacing:-.02em;
    }

    .lead{ color:var(--muted); font-size:16px; line-height:1.55; margin:0 0 16px; }
    .hero__cta{ display:flex; gap:12px; flex-wrap:wrap; margin:12px 0 12px; }

    .checks{
      list-style:none;
      padding:0;
      margin:10px 0 8px;
      display:grid;
      gap:8px;
      color:var(--muted);
      font-weight:600;
      font-size:13px;
    }
    .checks li::before{ content:"✓"; color:rgba(120,255,170,.85); margin-right:10px; }

    .note{ margin:0; color:rgba(255,255,255,.55); font-size:12px; }

    .hero__visual{ position:relative; }

    .window{
      background:rgba(255,255,255,.04);
      border:1px solid rgba(255,255,255,.10);
      border-radius:18px;
      overflow:hidden;
      box-shadow:0 0 40px rgba(0,0,0,.35);
    }

    .window__top{
      display:flex;
      align-items:center;
      gap:8px;
      padding:10px 12px;
      border-bottom:1px solid rgba(255,255,255,.08);
      background:rgba(0,0,0,.18);
    }

    .dot{ width:10px; height:10px; border-radius:999px; background:rgba(255,255,255,.18); display:inline-block; }
    .window__title{ margin-left:6px; font-weight:700; font-size:12px; color:rgba(255,255,255,.75); }

    .window__chip{
      margin-left:auto;
      font-size:12px;
      padding:6px 10px;
      border-radius:999px;
      background:rgba(255,255,255,.06);
      border:1px solid rgba(255,255,255,.10);
      color:rgba(255,255,255,.8);
      font-weight:700;
    }

    .window__body{
      display:grid;
      gap:14px;
      padding:14px;
    }

    /* SINGLE layout (nur Bild) */
    .window__body--single{
      grid-template-columns: 1fr;
    }

    .window__left--single{
      width:100%;
    }

    .window__brand{
      font-weight:800;
      font-size:12px;
      color:rgba(255,255,255,.75);
      margin-bottom:10px;
    }

    .window__placeholder{
      border-radius:14px;
      border:1px dashed rgba(255,255,255,.14);
      background:
        radial-gradient(300px 180px at 70% 20%, rgba(76,201,255,.18), transparent 60%),
        radial-gradient(280px 160px at 30% 80%, rgba(120,255,170,.14), transparent 60%),
        rgba(255,255,255,.04);
      overflow:hidden;
    }

    .window__placeholder--img{
      height:260px; /* etwas größer für die Grafik */
    }

    .window__img{
      width:100%;
      height:100%;
      object-fit:cover;
      display:block;
    }

    .mini-metrics{
      margin-top:14px;
      display:grid;
      grid-template-columns: repeat(4, 1fr);
      gap:10px;
    }

    .metric{
      background:rgba(255,255,255,.04);
      border:1px solid rgba(255,255,255,.08);
      border-radius:14px;
      padding:10px 12px;
    }

    .metric b{ font-size:13px; }
    .metric span{ display:block; color:rgba(255,255,255,.60); font-weight:700; font-size:12px; margin-top:4px; }

    /* Sections */
    .section{ padding:46px 0; }
    .section--tight{ padding:26px 0 40px; }
    h2{ margin:0 0 10px; font-size:28px; letter-spacing:-.02em; }
    h3{ margin:0 0 8px; }
    .muted{ color:var(--muted); }

    .impact{
      margin-top:18px;
      padding:18px;
      border-radius:18px;
      border:1px solid rgba(255,255,255,.10);
      background:rgba(255,255,255,.04);
      box-shadow:0 0 30px rgba(0,0,0,.25);
    }

    .impact__number{
      display:flex;
      align-items:baseline;
      gap:10px;
      padding-top:6px;
    }

    .impact__number .n{ font-size:22px; font-weight:700; letter-spacing:-.03em; }
    .impact__number .u{ font-size:22px; font-weight:900; color:rgba(120,255,170,.85); }
    .impact__number .s{ font-size:18px; font-weight:800; color:rgba(255,255,255,.65); }

    /* Image cards with HTML text overlay */
    .cards{
      margin-top:18px;
      display:grid;
      grid-template-columns: repeat(4, 1fr);
      gap:14px;
    }

    .imgcard{
      position:relative;
      min-height:280px;
      border-radius:18px;
      overflow:hidden;
      border:1px solid rgba(255,255,255,.10);
      background:
        linear-gradient(180deg, rgba(0,0,0,.00), rgba(0,0,0,.45)),
        var(--bg);
      background-size:cover;
      background-position:center;
      box-shadow:0 0 30px rgba(0,0,0,.25);
    }

    .imgcard__overlay{
      position:absolute;
      inset:0;
      display:flex;
      flex-direction:column;
      justify-content:flex-end;
      padding:16px;
      gap:10px;
      background:linear-gradient(180deg, rgba(0,0,0,.00) 40%, rgba(0,0,0,.55) 100%);
    }

    .imgcard h4{
      margin:0;
      font-size:20px;
      font-weight:900;
      text-shadow:0 2px 10px rgba(0,0,0,.45);
    }

    .imgcard ul{
      margin:0;
      padding-left:16px;
      color:rgba(255,255,255,.78);
      font-weight:700;
      font-size:13px;
      line-height:1.35;
    }

    /* grids */
    .grid3{
      display:grid;
      grid-template-columns: repeat(3, 1fr);
      gap:14px;
      margin-top:14px;
    }

    .grid2{
      display:grid;
      grid-template-columns: 1fr 1fr;
      gap:14px;
      margin-top:14px;
    }

    .panel{
      background:rgba(255,255,255,.04);
      border:1px solid rgba(255,255,255,.10);
      border-radius:18px;
      padding:16px;
    }

    .panel p{ color:rgba(255,255,255,.72); margin:0; line-height:1.5; }
    .bullets{ margin:10px 0 0; color:rgba(255,255,255,.72); font-weight:700; line-height:1.45; }
    .mt{ margin-top:26px; }

    .steps{
      margin-top:14px;
      display:grid;
      grid-template-columns: repeat(3, 1fr);
      gap:14px;
    }

    .step{
      background:rgba(255,255,255,.04);
      border:1px solid rgba(255,255,255,.10);
      border-radius:18px;
      padding:16px;
    }

    .step__n{
      width:34px; height:34px; border-radius:12px;
      display:flex; align-items:center; justify-content:center;
      font-weight:950;
      background:rgba(76,201,255,.14);
      border:1px solid rgba(76,201,255,.25);
      margin-bottom:10px;
    }

    .step h4{ margin:0 0 6px; font-size:16px; }
    .step p{ margin:0; color:rgba(255,255,255,.72); line-height:1.5; font-weight:650; }

    .cta{
      margin-top:18px;
      display:grid;
      grid-template-columns: 1.1fr .9fr;
      gap:14px;
      align-items:stretch;
    }

    .cta__text{
      background:rgba(255,255,255,.04);
      border:1px solid rgba(255,255,255,.10);
      border-radius:18px;
      padding:18px;
    }

    .cta__btns{ display:flex; gap:12px; flex-wrap:wrap; margin-top:12px; }

    .form{
      background:rgba(255,255,255,.04);
      border:1px solid rgba(255,255,255,.10);
      border-radius:18px;
      padding:18px;
      display:grid;
      gap:12px;
    }

    label span{ display:block; font-weight:800; font-size:12px; color:rgba(255,255,255,.72); margin-bottom:6px; }

    input{
      width:100%;
      padding:12px 12px;
      border-radius:14px;
      border:1px solid rgba(255,255,255,.12);
      background:rgba(0,0,0,.20);
      color:rgba(255,255,255,.92);
      outline:none;
    }

    input:focus{ border-color:rgba(76,201,255,.35); box-shadow:0 0 0 3px rgba(76,201,255,.10); }

    .tiny{ font-size:12px; margin:0; }

    .faq{
      margin-top:12px;
      background:rgba(255,255,255,.04);
      border:1px solid rgba(255,255,255,.10);
      border-radius:16px;
      padding:12px 14px;
    }

    summary{ cursor:pointer; font-weight:900; }
    .faq p{ margin:10px 0 0; color:rgba(255,255,255,.72); line-height:1.5; }

    .footer{
      margin-top:22px;
      display:flex;
      align-items:center;
      justify-content:space-between;
      gap:12px;
      padding-top:14px;
      border-top:1px solid rgba(255,255,255,.10);
    }

    /* Responsive */
    @media (max-width: 768px){
  .hero__grid{ grid-template-columns: 1fr; }
  h1{ font-size:38px; }
  .nav{ flex-wrap:wrap; }
  .nav__right{ width:100%; display:flex; gap:10px; justify-content:flex-start; }
  .mini-metrics{ grid-template-columns: repeat(2, 1fr); }
  .cards{ grid-template-columns: repeat(2, 1fr); }
  .grid3{ grid-template-columns: 1fr; }
  .steps{ grid-template-columns: 1fr; }
  .cta{ grid-template-columns: 1fr; }
  .nav__center{ display:none; }

  /* Fenster im Hero: mehr Platz & sauberer Stack */
  .window__body{ grid-template-columns: 1fr; }
  .window__placeholder{ height:240px; }
}

.logo{
  filter: drop-shadow(0 1px 2px rgba(0,0,0,0.5))
          drop-shadow(0 0 6px rgba(0,0,0,0.35));
}

@media (max-width: 520px){
  h1{ font-size:32px; }
  .lead{ font-size:15px; }
  .pill{ font-size:11px; }
  .hero__cta{ gap:10px; }

  /* Buttons etwas größer klickbar */
  .btn{ padding:12px 14px; font-size:14px; }

  /* Karten untereinander */
  .cards{ grid-template-columns: 1fr; }
  .imgcard{ min-height:240px; }

  /* Navigation-Logo größer und sauber */
  .logo{
  max-width: 160px;
  width: 100%;
  height: auto;
  object-fit: contain;
}
.logo_wrap{
  display: flex;
  align-items: center;
}

  /* Container enger, damit es nicht gequetscht wirkt */
  .container{ width:calc(100% - 28px); }
  .nav{ width:calc(100% - 28px); }
  .hero{ width:calc(100% - 28px); }
}
  </style>
</Layout>
